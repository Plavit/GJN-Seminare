<?php/*Hlavni strankaFunkcnost:-Zobrazi formulare "login" a "registrace"-Kontroluje vstupy-Funguje jako "obalka" pro dalsi obsah (content)*/session_start();?><!DOCTYPE html><html>    <head>        <meta charset="utf-8">        <title>Registrační systém</title>        <link rel="shortcut icon" href="images/favicon.ico" type="image/x-icon">        <link rel="stylesheet" href="style.css" type="text/css">    </head>    <body>        <div id=container>        <div class="line"></div><?phpinclude_once "horni.php";if(empty($_SESSION["userID"])){	include 'form.html';}else{?><div class="obsah-container"><div class="obsah"><?php	require_once 'scripts/db-connect.php';	require_once 'scripts/tools.php';	$stmt = $dbI->prepare("SELECT realname FROM `".$prefix."Users` WHERE userID = ?");	$stmt->bind_param("i", $_SESSION["userID"]);	$stmt->bind_result($realName);	$stmt->execute();	$stmt->fetch();	$stmt->close();	if(!isAdmin($_SESSION["userID"])){		$dstmt = $dbI->prepare("SELECT older FROM `".$prefix."Users` WHERE userID = ?");		$dstmt->bind_param("s",$_SESSION["userID"]);		$dstmt->bind_result($older);		$dstmt->execute();		$dstmt->fetch();		$dstmt->close();		echo "Vítej ".$realName." na aplikaci pro registraci seminářů.<brs>";		if($older==1){			$piece = 2;		}else{			$piece = 1;		}		$stmt = $dbI->prepare("SELECT seminarID,title,teacher,description FROM `".$prefix."Seminars` WHERE deactivated = 0 AND (access = 0 OR access = ?)");		$stmt->bind_param("s",$piece);		$stmt->bind_result($seminarID,$title,$teacher,$description);		$stmt->execute();		echo "<script>function isValid(){if(document.querySelectorAll('input[type=\"checkbox\"]:checked').length>".($older==1?"7":"5")."){document.getElementById('err').innerHTML=\"Limit překročen. Limit činí pro studenty čtvrtéhoho ročníku 5 a pátého ročnku 7 seminářů.\"; return false;}}</script>";		echo "<form method=\"post\" action=\"seminars.php\" onsubmit=\"return isValid()\">";		while($stmt->fetch()){			$stmt->store_result();			$stmt2 = $dbI->prepare("SELECT * FROM `".$prefix."UserSeminar` WHERE seminarID = ? AND userID =?");			$stmt2->bind_param("ii", $seminarID, $_SESSION["userID"]);			$stmt2->execute();			$stmt2->store_result();			if($stmt2->num_rows==1){				$check = "checked";			}else{				$check = "";			}			echo "<div style=\"border: solid 1px black; padding: 5px; margin: 5px;\">".$title."<br><font-size=2em><i>".$teacher."</i></font>";			echo "<input type=\"checkbox\" name=\"".$seminarID."\" ".$check.">";			echo "<br>";			$stmt2->close();			echo $description."</div>";		}		echo "<p id=\"err\"></p><br>";		echo "<input type=\"submit\">";		echo "</form>";	}else{		echo '<h2>Administrátorské rozhraní</h2><br>        <table>		<th href="table.php">		<a href="tabler.php">Tabulka společných účastníků.</a><br>        </th>        <td>        t1        </td>        <th>		<a href="table-user.php">Tabulka společných seminářů.</a><br>        </th>		<a href="user-management.php">Správa uživatelů.</a><br>		<a href="seminar-management.php">Správa seminářů.</a><br>        </table>		</div>		</div>';	}	echo '<div class="obsah">	<br><a href="logout.php">Odhlásit se</a>	</div>';}?></div><?phpinclude ("paticka.php");?></body></html>